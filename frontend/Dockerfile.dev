# Development Dockerfile for Frontend
# Uses Vite dev server with hot-reload support

FROM node:22-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies (use npm install for dev flexibility)
RUN npm install

# Copy source files
COPY . .

# Expose Vite dev server port
EXPOSE 5173

# Start Vite dev server
# --host 0.0.0.0 allows external connections (Docker networking)
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
