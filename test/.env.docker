# Docker Environment
# Use this when running Cypress inside a Docker container
# Uses host network mode (network_mode: host) so Cypress can access localhost directly

# Frontend URL (Docker container shares host network)
CYPRESS_BASE_URL=http://localhost:5174

# Backend API URL (Docker container shares host network)
CYPRESS_API_URL=http://localhost:3000/api/v1

# Qase Test Management (optional)
# QASE_MODE=testops
# QASE_TOKEN=your_token_here
# QASE_PROJECT=your_project_code