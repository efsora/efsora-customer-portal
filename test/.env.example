# Qase.io Integration Configuration
#
# To use Qase test reporting, set these environment variables:
#
# 1. Get your API token from: https://qase.io/app/settings/team-tokens
# 2. Replace the placeholder value below with your actual token
# 3. Copy this file to .env.local (which is in .gitignore and not committed)
#
# For local development:
#   cp .env.example .env.local
#   Edit .env.local and add your actual token
#
# For CI/CD:
#   Set QASE_API_TOKEN, QASE_PROJECT, and optionally QASE_RUN_ID as environment variables

# Your Qase API token (required for test result uploads)
# Get from: https://qase.io/app/settings/team-tokens
QASE_API_TOKEN=your_token_here

# Qase project code (optional, defaults to ECP)
# This should match your project code in Qase
QASE_PROJECT=ECP

# Qase run ID (optional)
# If you want to report to an existing test run, provide its ID here
# If not set, a new test run will be created automatically
# QASE_RUN_ID=12345
