/**
 * Authentication Constants
 *
 * Centralized configuration for authentication-related values
 */

/**
 * JWT token expiration duration
 * Format: "7d" = 7 days, "24h" = 24 hours, "60m" = 60 minutes
 */
export const LOGIN_EXPIRES_AT = "7d";
